export interface Receiver {
    account: { address: string }
}
export interface TokensMerged {
    balance: string
    image: string
    tokenID: string
    name: string
    checked: boolean
    givingEachQuantity: number
}
